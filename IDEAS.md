# Verto 脑洞、试做计划与问题

这里是本项目唯一的脑洞、试做计划与问题入口。脑洞库收录尚未承诺实施的想法，试做计划收录已经具体到需要实施或验证的方案，问题表收录用户报告和已确认缺陷。正式决定和当前开发状态以代码、项目文档与实际验证证据为准。

## 记录规则

- 用户提出明确的产品脑洞、优化方案、功能修改或异常时，AI 直接按下方分类登记。单纯询问原理、术语或现状时先回答问题。讨论形成具体项目方向后再登记。
- 用户输入以 `记`、`记一下`、`记脑洞`、`记计划` 或 `记 Bug` 开头且没有指定其他位置时，AI 直接把内容追加到下方对应表格。
- 概念、愿望和初步设想写入脑洞库。已经包含具体目标或验证动作的改进、新增功能和验证方案写入试做计划。归类不明确时写入脑洞库。
- 用户报告异常、回归或待修复行为时，写入问题表。缺少现场复现证据时，确认程度记为 `用户报告`，状态记为 `待复现`，原因记为 `未确认`。
- 脑洞确定需要试做后，将原记录标为 `已转计划`，再新增一个 `TRY` 编号并在来源栏引用原 `IDEA` 编号。原始想法保留在脑洞库，试做方法、状态和证据只在计划表维护。
- 脑洞编号从 `IDEA-001` 开始递增，试做计划编号从 `TRY-001` 开始递增，问题编号从 `BUG-001` 开始递增。已有编号不改、不复用，默认按记录时间追加，不重新排序。
- 日期使用项目所在时区的 `YYYY-MM-DD`。脑洞状态默认为 `待整理`。计划细节未定时标为 `待细化`，已有明确验证方法时标为 `待验证`。问题状态默认为 `待复现`。状态变化必须来自用户的明确决定或实际证据。
- 试做计划和问题使用 `高`、`中`、`低` 三级优先级。`高` 表示核心功能不可用、存在数据风险或阻塞其他项目。`中` 表示明显影响正常功能或主要体验。`低` 表示细节改进、视觉调整或长期方向。优先级表示建议处理顺序，确认程度仍以证据栏为准。
- `IDEA` 不建立任务书。项目准备交给执行者时，`TRY` 或 `BUG` 在 `docs/work-items/` 下建立与编号同名的 Markdown 任务书，并在表格中链接。任务书定稿并进入 `main` 后才可交接。
- 执行者收到编号后先读取本文件和对应任务书，只处理任务书定义的范围。实施完成后回填任务书中的分支、提交、改动、验证证据与未完成项，并同步本表状态。编译成功只证明代码能构建。界面和动效需要真实屏幕证据。
- 是否并行根据文件重叠、数据迁移、依赖关系和合并顺序逐项判断。存在直接依赖或修改同一功能区域的项目按顺序执行。
- 每次新增或修改记录时，同时使用 `$talk-like-scarletkc` 和 `$marketing-copy`。`$talk-like-scarletkc` 采用 Project writing 中技术报告和实验记录的语气，保留用户的原意、判断和明确立场，同时使用标准术语、统一状态标签和严肃专业的表达。
- `$marketing-copy` 负责事实与公开边界。已交付、计划中、纯脑洞和待验证结论必须明确区分。功能效果、数字、比较和时间承诺需要可核对的证据。证据不足时标为 `待验证`，结论栏留空。
- 表格正文使用具体、克制的项目记录表达。宣传口号、夸张形容词、聊天口头禅、情绪放大、刻意比喻和 AI 式总结一律省略。
- 本仓库是公开仓库，`IDEAS.md` 和任务书按可公开项目文档处理。涉及密钥、邀请码、私人联系方式、保密资料或未公开 URL 时，AI 说明具体风险并等待用户指定安全位置，公开文档仅记录可公开摘要。
- 内容里的表格竖线 `|` 统一写成 `\|`。

## 脑洞库

状态统一使用 `待整理`、`保留`、`已转计划`、`搁置` 或 `已放弃`。

| 编号 | 日期 | 内容 | 状态 | 后续线索 / 备注 |
| --- | --- | --- | --- | --- |
| IDEA-001 | 2026-08-27 | 将语言转换栏设计为跨翻译场景持续存在的浮动界面锚点。切换场景时，控件根据当前页面的信息与操作需求变形，持续呈现源语言、目标语言、交换等关键信息和操作。连续的形状变化帮助用户识别其位置与最新形态。 | 待整理 | 核心判断是语言转换栏属于每个翻译场景的共同核心。当前导航仍使用三个独立标签页，后续需要明确各场景形态、信息层级、操作边界和变形规则。 |
| IDEA-002 | 2026-08-27 | 以现有界面元素的连续变形完成翻译场景转换。文字翻译切换到拍照翻译时，`Text Translate` 标题变为 `Vision Translate`，文字输入框扩大为取景框，其他相关元素同步变形。切换动画支持中途打断和返回，线条沿路径重组界面作为候选表现方式。 | 待整理 | 关联 `IDEA-001`。当前属于初期方向。现有场景切换由系统 `TabView` 承担，尚未建立元素映射、变形路径、动画中断和状态恢复规则。 |
| IDEA-003 | 2026-08-28 | 将 `PP-OCR-for-Apple` 改名并扩展定位为 Verto 使用的模型包仓库。仓库负责记录上游来源，转换、验证并发布 Verto 所需的设备端模型包，名称覆盖 OCR、图像修复、语音听写及后续模型类型。每个模型继续单独保留来源、许可证、转换方法、兼容范围和验证证据。 | 待整理 | 当前推荐名称为 `Verto-Model-Packs`。候选还包括 `Verto-Model-Assets`、`Verto-On-Device-Models` 与 `Verto-Model-Foundry`。现有仓库主要包含 PP-OCR 的来源锁、转换配方、清单、评测、ANE 报告和发布包。改名前需要盘点 Verto 的 `OCRModelPack.releaseBaseURL`、五语 README、源码链接、Release、工作流与公开文档引用，并在改名后更新规范地址和验证下载链路。公开说明继续逐项标注模型的上游来源和归属。 |
| IDEA-004 | 2026-08-29 | 建立 Verto 开源版与商业版之间可持续的更新同步机制，并为两边提供统一的可下载本地翻译模型目录。开源版后续移除在线 API 依赖，允许用户按需下载 `MADLAD-400 3B`、`M2M-100 418M`、`NLLB-200 distilled 600M`、`TranslateGemma 4B` 和 `SeamlessM4T v2`；商业版 App 同步接入这些模型，具体权限、分发和默认路线后续再定。 | 待整理 | 先确定双仓的共享核心、商业差异、同步方式和变更回送边界，再拆分模型来源与许可证核验、语言覆盖、转换到 Core ML、存储与内存、下载更新与删除、翻译质量、速度和真机温度评测等任务。模型目录需要保留版本、来源、许可证、文件大小、SHA-256、支持语言、系统要求和已验证设备范围。当前只记录方向，不开始实现或下载模型。 |

## 试做计划

这里收录用户已经明确提出的改进、修改、新增功能和验证方案。状态统一使用 `待细化`、`待验证`、`验证中`、`已验证`、`暂停` 或 `已取消`。完成验证后，在结果栏记录实际结果、证据位置和仍未确认的边界。

| 编号 | 日期 | 来源 | 优先级 | 验证目标 | 验证方法 | 状态 | 任务书 | 结果 / 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TRY-001 | 2026-08-27 | 用户提出 | 中 | 拍照翻译完成拍摄后，在靠近底部的位置提供退出操作。原快门控件变形为退出按钮，现有右上角退出按钮移除。 | 在现有快门控件上实现结果态变形。验证拍摄前后的控件位置与辅助功能语义、退出后的页面重置行为，以及结果态的右上角退出入口是否消失。 | 待验证 | - | 当前结果态隐藏底部快门并使用右上角 `camera.retakeButton`，目标行为尚未实现。 |
| TRY-002 | 2026-08-27 | `BUG-002`、用户提出 | 中 | 主页语音识别入口在空闲状态显示话筒图标。启动识别后，现有控件变形为由数条竖杠组成的波形，竖杠高度随输入声音变化。 | 在现有话筒控件上实现空闲态与识别态转换。验证点击后的即时状态反馈、实际输入音量变化对应的波形变化，以及识别停止后恢复话筒图标。 | 待验证 | - | 当前源码已由 `VoiceConversationView.micButtonContent` 在聆听状态显示 `WaveBars`，并由 `VoiceConversationController.audioLevel` 提供真实输入电平。实际屏幕表现仍待验证。 |
| TRY-003 | 2026-08-27 | 用户反馈 | 中 | 重新梳理拍照翻译页面的界面结构，降低当前元素堆叠造成的混乱，建立清晰的信息与操作层级。 | 待制定。后续先盘点现有控件、页面状态和主要操作，再形成详细优化计划与验收边界。 | 待细化 | - | 当前页面已经采用顶部语言和结果模式控件、顶部处理状态、底部拍摄控件与单一结果画布。原始范围需要结合当前界面重新细化。当前结构见 [`docs/photo-translation-architecture.md`](docs/photo-translation-architecture.md)。 |
| TRY-004 | 2026-08-27 | 用户提出 | 低 | 语音对话设置中的模型选择项以文字显示当前模型名称。 | 修改现有模型选择控件的显示内容，验证可选模型、当前选择状态及选择后的文字更新。 | 待细化 | - | 当前 `SettingsView.engineSection` 已用文字显示翻译引擎名称。目标是否指语音页面内的其他模型控件仍需明确。 |
| TRY-005 | 2026-08-27 | 用户提出 | 低 | 删除设置中的 `自研模型` 选项。 | 移除现有设置入口及相关不可选状态展示，检查设置页面、本地化资源和代码引用中的遗留内容。 | 待验证 | - | 当前 `TranslationEngine.custom` 与对应设置行仍存在，设置项处于不可选状态。 |
| TRY-006 | 2026-08-27 | 用户提出 | 中 | 新增历史记录删除功能，允许用户移除不再需要的翻译记录。 | 待制定。后续明确单条删除、批量删除、全部清空及确认或恢复行为的产品范围。 | 待细化 | - | 当前 `HistoryView` 提供载入与收藏操作，`TranslationSession` 没有删除历史记录的方法。该项目与 `BUG-004` 修改同一功能区域，应按顺序执行。 |
| TRY-007 | 2026-08-28 | 用户提出 | 中 | 为文字翻译页寻找可在 iPhone 本地运行的实时语音听写模型。模型必须持续接收麦克风音频并及时返回增量文字与最终文字。Whisper 模型家族及其蒸馏、重打包版本全部排除。主要决策题为候选模型是否值得部署到 Apple Neural Engine（ANE，通常称为 NPU）执行。 | 先筛选具备实时流式接口、iOS 部署条件与可接受许可证的非 Whisper 候选。固定真机基准为 iPhone XR、iPhone 12 与 iPhone 16 Pro，临时可用的其他设备作为补充样本。候选模型使用同一批真实录音和现场麦克风输入，与当前 Apple 听写路径比较准确率、首段文字延迟、最终结果延迟、连续听写稳定性、内存、耗电和温度。ANE 结论需要实际执行证据，并在条件允许时比较同一模型的 CPU、GPU 与 ANE 路径。测试范围保持有界，目标是回答该模型是否值得在 NPU 上运行。 | 待细化 | - | 当前 `main` 未包含 Whisper。生产路径由 `SpeechEngineFactory` 在 iOS 26 以上优先使用 `SpeechAnalyzer`，其他环境使用 `SFSpeechRecognizer`。过去的 Whisper 设备测试不进入本轮候选结果，已有测试夹具、真机连接经验和指标口径可以复用。模型成功转换或编译为 Core ML 不能证明实际使用 ANE，也不能证明其适合实时听写。 |
| TRY-008 | 2026-08-28 | 用户提出 | 中 | 将当前语言选项中的 `中文` 明确为 `简体中文`，新增 `繁體中文`，并支持简体中文与繁体中文双向转换。Verto 内部统一使用 `zh-Hans` 与 `zh-Hant`，不使用地区代码表示简体或繁体。 | 待制定。语言选择、自动检测、OCR、语音、历史记录、设置恢复、缓存键、测试数据和 Verto 中转协议统一保存 `zh-Hans` 或 `zh-Hant`。外部服务要求的地区代码限制在对应供应商适配器内，请求前转换，响应后立即恢复为 Verto 代码。验收覆盖简繁双向转换、其他语言互译和持久化数据。 | 待细化 | - | 当前语言模型只定义 `zh-Hans`，界面自称显示为 `中文`。Google Cloud 普通 NMT 的繁体中文传输格式仍使用供应商地区代码，因此该映射需要隔离在 Google 服务端适配器，不能进入 App 产品模型或 Verto 中转协议。 |
| TRY-009 | 2026-08-28 | 用户提出 | 高 | 面向中国大陆网络环境使用可稳定访问的非 Google 翻译服务，其他地区保留当前提供商策略。 | 大陆默认供应商确定为阿里云机器翻译通用版，DeepL 定位为其他地区的高级提供商。先在中国大陆网络实测中转入口与阿里云服务的可达性、冷启动、端到端延迟、错误率和翻译质量。简体中文与繁体中文双向转换直接验收。阿里云官方限制繁体中文只与中文互译，因此其他语言与 `zh-Hant` 的互译方案需要在任务书中单独确定并验证。Verto 继续统一使用 `zh-Hans` 与 `zh-Hant`，阿里云的 `zh`、`zh-tw` 只在供应商适配器边界转换。提供商切换和失败行为需要对用户可解释。 | 待细化 | - | 产品决定：大陆线路选择阿里云，百度从候选、对照测试和回退列表中排除。阿里云通用版官方资料列出 214 种语言、每月 100 万字符免费额度和 50 元/百万字符后付费价格。繁体中文只支持与中文互译。DeepL 使用 `ZH-HANT`，保留为高级计划候选。大陆线路同时需要境内可达的中转入口，供应商和中转入口分别验收。关联 `TRY-008`、`BUG-005`。 |
| TRY-010 | 2026-08-28 | 用户提出 | 高 | 拍照翻译以照片像素和文字内容为唯一方向来源。手机可以绕镜头方向旋转完整 360 度，也可以竖持、横持、倒置、倾斜或平放拍摄。系统自行识别文字阅读方向，译文逐行贴合原文四边形，位置、基线方向和透视方向与照片一致。 | 该项已转由 `BUG-006` 追踪。后续专项对话以 `BUG-006` 为入口制定修复方案和验收范围。 | 暂停 | - | 当前实现与产品要求的冲突已有代码证据，后续记录见 `BUG-006`。保留本行及编号。 |
| TRY-011 | 2026-08-28 | 用户提出 | 中 | 正式构建在确认无网络连接时，将文字翻译与拍照翻译请求切换到设备上的 Apple Translation 框架。已安装对应语言资源时直接在本机完成翻译，并向用户显示当前使用系统离线翻译。 | 待制定。真机在飞行模式下分别验证已安装语言资源、缺少语言资源、不受支持语言对与不受支持系统版本。已安装资源时需要确认翻译完成且没有网络请求。资源缺失或系统不可用时显示可执行的说明，翻译流程结束等待状态。网络恢复后的新请求回到用户原先选择的在线提供商，已经生成的离线结果保持不变。 | 待细化 | - | 项目已包含 `AppleTranslationProvider` 与 `AppleBackedTranslationService`。当前 `TranslationEngine.makeService` 在配置中转时固定返回 `TranslationRelayService`，网络失败后不会调用现有 Apple 离线路径。Apple 路径当前要求真机、iOS 18 或以上及已安装的对应语言资源。代码中尚未发现负责正式翻译路由的网络状态监测。语音页面已有独立的 Apple 优先路由，本项范围为文字翻译与拍照翻译。 |
| TRY-012 | 2026-08-28 | 用户提出 | 中 | 为 Verto 增加更多语言。新增候选语言的上限由 PP-OCRv6 Small 识别模型的明确语言范围决定，候选列表保持在该模型支持的语言与文字体系内。 | 先在模型仓库建立 PP-OCRv6 Small 的权威语言代码白名单，再从中选择新增批次。每种候选语言需要确认规范语言代码、原生名称、界面名称、在线提供商映射、Apple 离线翻译可用性、Small 拍照识别、Vision 回退、历史记录和缓存键。按功能分别记录能力边界，新增语言需要完成真实文字图片与翻译链路验证。 | 待细化 | - | 当前 `Language.all` 包含英语、简体中文、日语、韩语、法语、西班牙语和德语。模型仓库的 `MODEL_CARD.md` 说明 Small 覆盖中文、日文、希腊文和拉丁文字语言，缺少谚文、西里尔、阿拉伯、泰文与天城文。当前发布清单记录 Small 字表含 18708 个字符，但尚未提供明确语言代码列表。`OCRModelTier.recognizes` 目前只排除韩语，其余语言代码默认通过，扩展前需要改为明确白名单。现有韩语依赖 Vision，是否继续作为既有例外留待细化。关联 `TRY-008`、`IDEA-003`。 |

## 问题

状态统一使用 `待复现`、`已复现`、`修复中`、`待验证`、`已解决` 或 `暂停`。用户报告与现场确认分开记录，原因和影响范围以实际证据为准。

| 编号 | 日期 | 问题 | 环境 / 触发条件 | 确认程度 | 优先级 | 状态 | 任务书 | 结果 / 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BUG-001 | 2026-08-27 | 进入拍照翻译页面后，其他页面的按钮全部变黑。 | iOS 26 以下、不支持 Liquid Glass 底部栏的设备。 | 用户报告 | 高 | 待复现 | - | 当前相机浮层的浅色语义环境已限制在页面内部控件。尚无 iOS 17 至 iOS 25 当前构建的屏幕证据，原因未确认。 |
| BUG-002 | 2026-08-27 | 点击主页语音识别话筒后，界面没有动画反馈，交互状态缺少可见变化。 | 主页语音识别入口，点击话筒后。 | 用户报告 | 中 | 待复现 | - | 当前源码已包含真实输入电平驱动的 `WaveBars`。尚未用当前构建和真实麦克风确认报告是否仍可复现。 |
| BUG-003 | 2026-08-27 | 拍照翻译显示翻译完成，文字块也可以点击查看翻译详情，但结果画布没有显示翻译后的画面。 | 完成一次拍照翻译并进入结果态后。 | 用户报告 | 高 | 待复现 | - | 当前源码已通过 `PhotoTranslationController.translatedImage` 原子发布最终图片，并有 `testFinalImagePublishesOnlyAfterAtomicReconstructionCompletes` 单元测试。视觉重建允许文字块进入 `unresolvedBlockIDs` 并保留原图，这条路径与用户报告吻合。真实照片仍待定向复现。当前结构见 [`docs/photo-translation-architecture.md`](docs/photo-translation-architecture.md)。 |
| BUG-004 | 2026-08-27 | 历史记录中始终存在三条 Demo 时期的演示翻译。 | 打开历史记录页面。 | 代码确认 | 中 | 已复现 | [BUG-004](docs/work-items/BUG-004.md) | `TranslationSession.historyItems` 当前直接使用 `HistoryItem.today + HistoryItem.yesterday` 初始化，共三条静态演示记录。原因已确认。 |
| BUG-005 | 2026-08-28 | 提交文字翻译后，用户体感返回速度慢于 Google Translate。 | 使用当前包含 Verto 翻译中转配置的构建。具体文本、网络条件、中转冷启动状态与耗时待记录。 | 用户报告 | 中 | 待复现 | - | 当前构建的实际路线为 App、Cloudflare Worker 中转、Google Cloud Translation v3。`LLM 翻译` 处于不可用状态，服务端当前调用 Cloud Translation v3。尚未采集端到端耗时，原因未确认。 |
| BUG-006 | 2026-08-28 | 拍照翻译方向判断依赖设备姿态传感器，无法满足手机绕镜头任意旋转或平放拍摄时仅依据图像内容识别文字方向并保持译文贴合的产品要求。 | 当前相机拍摄路径。任意角度旋转手机、平放拍摄桌面文字或斜持拍摄时均属于影响范围。 | 代码确认 | 高 | 已复现 | - | `PhotoCaptureSource` 使用 `AVCaptureDevice.RotationCoordinator` 取得重力方向，`OrientationSnapshot.contentQuarterTurns` 据此生成 90 度整数倍修正，并传入 `PhotoTranslationController.runRecognition`。现有测试覆盖四个标准方向，缺少传感器退出链路、360 度图像内容判向和完整斜拍译图证据。`TextQuad` 与 `AdaptiveBackgroundReconstructor.makePatch` 已提供四边形坐标和透视贴回基础。后续由专项对话制定修复方案。关联 `TRY-010`、`BUG-003`。 |

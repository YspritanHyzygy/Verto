# Verto 脑洞、试做计划与问题

这里是本项目唯一的脑洞、试做计划与问题入口。脑洞库收录尚未承诺实施的想法，试做计划收录已经具体到需要实施或验证的方案，问题表收录用户报告和已确认缺陷。正式决定和当前开发状态以代码、项目文档与 `AGENTS.md` 为准。

## 记录规则

- 用户输入以 `记` 或 `记一下` 开头且没有指定其他位置时，AI 直接把内容追加到下方表格。
- 明确说 `记脑洞`，或内容仍处于概念、愿望和初步设想阶段时，写入脑洞库。明确说 `记计划`，或内容已经包含具体目标与验证动作时，写入试做计划。归类不明确时默认写入脑洞库。
- 用户明确提出 UI 优化、功能修改或新增功能时，写入试做计划。目标已经明确但实现与验证细节仍待设计时，状态记为 `待细化`，验证方法记为 `待制定`。
- 用户报告异常、回归或待修复行为时，写入问题表。缺少现场复现证据时，确认程度记为 `用户报告`，状态记为 `待复现`，原因记为 `未确认`。
- 脑洞确定需要试做后，将原记录标为 `已转计划`，再新增一个 `TRY` 编号并在来源栏引用原 `IDEA` 编号。原始想法保留在脑洞库，试做方法、状态和证据只在计划表维护。
- 每次新增或修改记录时，同时使用 `$talk-like-scarletkc` 和 `$marketing-copy`。`$talk-like-scarletkc` 采用 Project writing 中技术报告和实验记录的语气，保留用户的原意、判断和明确立场，同时使用标准术语、统一状态标签和严肃专业的表达。
- `$marketing-copy` 负责事实与公开边界。已交付、计划中、纯脑洞和待验证结论必须明确区分。功能效果、数字、比较和时间承诺需要可核对的证据。证据不足时标为 `待验证`，结论栏留空。
- 表格正文使用具体、克制的项目记录表达。宣传口号、夸张形容词、聊天口头禅、情绪放大、刻意比喻和 AI 式总结一律省略。
- 脑洞编号从 `IDEA-001` 开始递增，试做计划编号从 `TRY-001` 开始递增，问题编号从 `BUG-001` 开始递增。已有编号不改、不复用，默认按记录时间追加，不重新排序。
- 日期使用项目所在时区的 `YYYY-MM-DD`。脑洞状态默认为 `待整理`。计划细节未定时标为 `待细化`，已有明确验证方法时标为 `待验证`。问题状态默认为 `待复现`。状态变化必须来自用户的明确决定或实际执行证据。
- 本仓库是公开仓库，`IDEAS.md` 按可公开项目文档处理。涉及密钥、邀请码、私人联系方式、保密资料或未公开 URL 时，AI 说明具体风险并等待用户指定安全位置，表格仅记录可公开摘要。
- 内容里的表格竖线 `|` 统一写成 `\|`。

## 脑洞库

状态统一使用 `待整理`、`保留`、`已转计划`、`搁置` 或 `已放弃`。

| 编号 | 日期 | 内容 | 状态 | 后续线索 / 备注 |
| --- | --- | --- | --- | --- |
| IDEA-001 | 2026-08-27 | 将语言转换栏设计为跨翻译场景持续存在的浮动界面锚点。切换场景时，控件根据当前页面的信息与操作需求变形，持续呈现源语言、目标语言、交换等关键信息和操作。连续的形状变化帮助用户识别其位置与最新形态。 | 待整理 | 核心判断是语言转换栏属于每个翻译场景的共同核心。需要继续明确各场景形态、信息层级、操作边界和变形规则。 |
| IDEA-002 | 2026-08-27 | 以现有界面元素的连续变形完成翻译场景转换。文字翻译切换到拍照翻译时，`Text Translate` 标题变为 `Vision Translate`，文字输入框扩大为取景框，其他相关元素同步变形。切换动画支持中途打断和返回，线条沿路径重组界面作为候选表现方式。 | 待整理 | 关联 `IDEA-001`。当前属于初期方向。需要继续打磨元素映射、变形路径、动画中断、状态恢复和交互连续性。 |

## 试做计划

这里收录用户已经明确提出的改进、修改、新增功能和验证方案。状态统一使用 `待细化`、`待验证`、`验证中`、`已验证`、`暂停` 或 `已取消`。完成验证后，在结果栏记录实际结果、证据位置和仍未确认的边界。

| 编号 | 日期 | 来源 | 验证目标 | 验证方法 | 状态 | 结果 / 证据 |
| --- | --- | --- | --- | --- | --- | --- |
| TRY-001 | 2026-08-27 | 用户提出 | 拍照翻译完成拍摄后，在靠近底部的位置提供退出操作。原快门控件变形为退出按钮，现有右上角退出按钮移除。 | 在现有快门控件上实现结果态变形。验证拍摄前后的控件位置与辅助功能语义、退出后的页面重置行为，以及结果态的右上角退出入口是否消失。 | 待验证 |  |
| TRY-002 | 2026-08-27 | `BUG-002`、用户提出 | 主页语音识别入口在空闲状态显示话筒图标。启动识别后，现有控件变形为由数条竖杠组成的波形，竖杠高度随输入声音变化。 | 在现有话筒控件上实现空闲态与识别态转换。验证点击后的即时状态反馈、实际输入音量变化对应的波形变化，以及识别停止后恢复话筒图标。 | 待验证 |  |
| TRY-003 | 2026-08-27 | 用户反馈 | 重新梳理拍照翻译页面的界面结构，降低当前元素堆叠造成的混乱，建立清晰的信息与操作层级。 | 待制定。后续先盘点现有控件、页面状态和主要操作，再形成详细优化计划与验收边界。 | 待细化 |  |
| TRY-004 | 2026-08-27 | 用户提出 | 语音对话设置中的模型选择项以文字显示当前模型名称。 | 修改现有模型选择控件的显示内容，验证可选模型、当前选择状态及选择后的文字更新。 | 待验证 |  |
| TRY-005 | 2026-08-27 | 用户提出 | 删除设置中的 `自研模型` 选项。 | 移除现有设置入口及相关不可选状态展示，检查设置页面、本地化资源和代码引用中的遗留内容。 | 待验证 |  |
| TRY-006 | 2026-08-27 | 用户提出 | 新增历史记录删除功能，允许用户移除不再需要的翻译记录。 | 待制定。后续明确单条删除、批量删除、全部清空及确认或恢复行为的产品范围。 | 待细化 |  |

## 问题

状态统一使用 `待复现`、`已复现`、`修复中`、`待验证`、`已解决` 或 `暂停`。用户报告与现场确认分开记录，原因和影响范围以实际证据为准。

| 编号 | 日期 | 问题 | 环境 / 触发条件 | 确认程度 | 状态 | 结果 / 证据 |
| --- | --- | --- | --- | --- | --- | --- |
| BUG-001 | 2026-08-27 | 进入拍照翻译页面后，其他页面的按钮全部变黑。 | iOS 26 以下、不支持 Liquid Glass 底部栏的设备。 | 用户报告 | 待复现 | 原因未确认。 |
| BUG-002 | 2026-08-27 | 点击主页语音识别话筒后，界面没有动画反馈，交互状态缺少可见变化。 | 主页语音识别入口，点击话筒后。 | 用户报告 | 待复现 | 原因未确认。 |
| BUG-003 | 2026-08-27 | 拍照翻译显示翻译完成，文字块也可以点击查看翻译详情，但结果画布没有显示翻译后的画面。 | 完成一次拍照翻译并进入结果态后。 | 用户报告 | 待复现 | 原因未确认。 |
| BUG-004 | 2026-08-27 | 历史记录中始终存在三条 Demo 时期的演示翻译。 | 打开历史记录页面。 | 用户报告 | 待复现 | 原因未确认。目标状态为不再自动出现这三条演示记录。 |

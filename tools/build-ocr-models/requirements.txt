# 转换链路：官方 ONNX → 改写 auto_pad → onnx2torch → coremltools。
# 版本是 2026-08 实测通过的组合。paddlepaddle 只用来跑 paddle2onnx 的依赖检查，
# 转换本身走官方已导出的 ONNX，不需要重新从 Paddle 权重导出。
#
# Python 必须是 3.12：coremltools/onnx2torch 在 3.13+ 上缺 wheel。
#   uv venv --python 3.12 .venv && source .venv/bin/activate
#   uv pip install -r requirements.txt
coremltools==9.0
huggingface_hub
numpy
onnx==1.17.0
onnx2torch==1.5.15
onnxruntime==1.28.0
pyyaml
torch==2.13.0
